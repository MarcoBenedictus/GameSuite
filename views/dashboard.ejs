<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Dashboard</title>
  <!-- Import Kanit font -->
  <link href="https://fonts.googleapis.com/css2?family=Kanit&display=swap" rel="stylesheet">
  <link rel="icon" type="image/png" href="/images/gamesuitelogo.png">
  <style>
    body {
      background-color: #778dfc;
      font-family: 'Kanit', sans-serif;
      margin: 0;
      padding: 0;
      color: white;
      position: relative;
      height: 100vh;
    }

    .top-bar {
      position: relative;
      width: 100%;
      height: 120px; /* Enough space to place items */
    }

    .logo-container {
      position: absolute;
      top: 0px;
      left: 0px;
    }

    .logo {
      width: 250px; /* Change size freely */
      height: auto;
    }

    .top-links {
      position: absolute;
      top: 30px;
      right: 40px;
      display: flex;
      gap: 20px;
    }

    .top-links a {
      text-decoration: none;
      color: white;
      font-weight: bold;
    }

    .top-links a:hover {
      opacity: 0.7;
    }

    .welcome-text {
      position: absolute;
      top: 90px;
      left: 50%;
      transform: translateX(-50%);
      text-align: center;
      font-size: 1.5rem;
    }

    /* Buttons container */
    .button-container {
      position: relative;
      width: 100%;
      height: 400px; /* Space for positioning buttons */
    }

    .ruangan-button,
    .membership-button {
      position: absolute;
      cursor: pointer;
      transition: transform 0.2s ease;
      transition: filter 0.3s ease;
    }

    /* Individual button positioning */
    .ruangan-button {
      width: 720px; /* Adjust width */
      height: auto;
      top: 300px; /* Change position */
      left: 0px; /* Adjust position */
    }

    .membership-button {
      width: 800px; /* Adjust width */
      height: auto;
      top: 300px; /* Change position */
      right: 0px; /* Adjust position */
    }

    /* On hover, buttons will slightly grow */
    .ruangan-button:hover,
    .membership-button:hover {
      transform: scale(1.05);
      filter: brightness(85%);
    }
  </style>
</head>
<body>

  <!-- Top Bar -->
  <div class="top-bar">
    <!-- Logo wrapped in its own container -->
    <div class="logo-container">
      <img src="/images/gamesuitelogoleft.png" alt="Game Suite Logo" class="logo" />
    </div>

    <!-- Navigation Links -->
    <div class="top-links">
      <a href="/misc/aboutus">❓About Us</a>
      <a href="/misc/tombolbantuan">🚨Tombol Bantuan</a>
      <a href="/misc/inbox">💬 Open Chat</a>
      <a href="/logout">🚪Logout</a>
    </div>
  </div>

  <!-- Welcome Message -->
  <div class="welcome-text">
    <h1>Welcome, <%= username %>!</h1>
    <p>You are logged in.</p>
  </div>

  <!-- Buttons -->
  <div class="button-container">
    <a href="/ruangan/ruanganlanding">
      <img src="/images/ruanganbutton.png" alt="Ruangan Button" class="ruangan-button" />
    </a>
    <a href="/membership/membershiplanding">
      <img src="/images/membershipbutton.png" alt="Membership Button" class="membership-button" />
    </a>
  </div>

  <div style="position: absolute; top: 30px; left: 67%; transform: translateX(-50%);">
  <a href="/myreservations" style="text-decoration: none; color: white; font-weight: bold; background: #3e4a95; padding: 10px 20px; border-radius: 10px;">
    📅 View My Reservations
  </a>
</div>


</body>
</html>
