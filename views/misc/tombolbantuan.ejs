<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Bantuan</title>
  <!-- Import Kanit font -->
  <link href="https://fonts.googleapis.com/css2?family=Kanit&display=swap" rel="stylesheet">
  <link rel="icon" type="image/png" href="/images/gamesuitelogo.png">
  <style>
    body {
      background-color: #778dfc;
      font-family: 'Kanit', sans-serif;
      margin: 0;
      padding: 0;
      color: white;
      position: relative;
      height: 100vh;
      overflow: hidden; /* Prevent overflow if the logo is moved */
    }

    /* Logo container (adjustable position and size) */
    .logo-container {
      position: absolute;
      top: 0px; /* Adjustable top position */
      left: 0px; /* Adjustable left position */
      width: auto;
      height: auto;
    }

    .logo {
      width: 250px; /* Adjustable size */
      height: auto;
    }

    /* Content container (adjustable position) */
    .content {
      position: absolute; /* Allow independent positioning */
      top: 100px; /* Adjustable top position for the content */
      left: 50%; /* Center horizontally */
      transform: translateX(-50%); /* Keep content centered horizontally */
      text-align: center;
    }

    /* Title */
    .title {
      font-size: 2rem;
      margin-top: 0; /* Adjust margin to your liking */
    }

    /* Member List */
    .member-list {
      font-size: 1.2rem;
      margin-top: 30px; /* Adjust spacing from title */
    }

    .member-list ul {
      list-style-type: none;
      padding: 0;
    }

    .member-list li {
      margin: 10px 0;
    }

    /* Back Button with adjustable position and size */
    .about-back-btn {
      position: absolute;
      bottom: -400px;
      left: 870px;
      display: block;
      width: 120px;
      height: auto;
      transition: transform 0.3s ease;
    }

    .about-back-btn:hover {
      transform: scale(1.05);
    }

    .about-back-btn img {
      width: 100%;
      height: auto;
      display: block;

    }
  </style>
</head>
<body>

  <!-- Logo -->
  <div class="logo-container">
    <img src="/images/gamesuitelogoleft.png" alt="Game Suite Logo" class="logo" />
  </div>

  <!-- Page Content -->
  <div class="content">
    <div class="title">
      <h1>Bantuan</h1>
    </div>

    <!-- Member List -->
    <div class="member-list">
      <h2>Contact Us:</h2>
      <ul>
        <li>WhatsApp : 000012340123</li>
        <li>Phone : 000011112222</li>
        <li>Email : test@gmail.com</li>
      </ul>
    </div>

  <!-- Back Button -->
  <a href="/dashboard" class="about-back-btn">
    <img src="/images/backbutton.png" alt="Back Button">
  </a>

</body>
</html>
