<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <title>Register</title>
  <link href="https://fonts.googleapis.com/css2?family=Kanit&display=swap" rel="stylesheet" />
  <link rel="icon" type="image/png" href="/images/gamesuitelogo.png">
  <style>
    html,
    body {
      margin: 0;
      padding: 0;
      height: 100%;
      overflow: hidden;
      background: url("/images/userregister.png") no-repeat center center;
      background-size: contain;
      background-color: #778dfc;
      font-family: 'Kanit', sans-serif;
    }

    .form-container {
      position: absolute;
      top: 34.5%;
      left: 21.5%;
      width: 50%;
      height: 50%;
      display: flex;
      flex-direction: column;
      justify-content: center;
      gap: 110px;
      position: relative;
    }

    input {
      font-size: 1.5rem;
      border: none;
      border-radius: 30px;
      padding: 15px 20px;
      background: transparent;
      color: #1b286b;
      outline: none;
      caret-color: black;
      font-family: 'Kanit', sans-serif;
    }

    input::placeholder {
      color: #1b286b;
      opacity: 0.8;
    }

    .error-message {
      color: red;
      text-align: center;
      position: absolute;
      bottom: -30px;
      left: 1.7%;
      font-size: 1.2rem;
      margin-top: 10px;
      white-space: nowrap;
    }

    a {
      color: white;
    }

    .register-button-wrapper {
      position: absolute;
      bottom: 40px;
      left: 50%;
      transform: translateX(-50%);
      z-index: 10;
    }

    button {
      width: 600px;
      height: 100px;
      background-image: url("/images/registerbutton.png");
      background-size: cover;
      background-repeat: no-repeat;
      background-position: center;
      background-color: transparent;
      border: none;
      transition: filter 0.3s ease;
      cursor: pointer;
    }

    button:hover {
      filter: brightness(85%);
    }

    .login-link {
      position: absolute;
      bottom: 20px;
      left: 50%;
      transform: translateX(-50%);
      text-align: center;
      font-size: 1.5rem;
      color: white;
      font-family: 'Kanit', sans-serif;
    }
  </style>
</head>

<body>
  <form id="register-form" class="form-container" action="/registlogin/register" method="POST">
    <input type="email" name="email" placeholder="Email" required />
    <input type="text" name="username" placeholder="Username" required />
    <input type="password" name="password" placeholder="Password" required />

    <% if (typeof error !== 'undefined' && error) { %>
    <p class="error-message">
      <%= error %>
    </p>
    <% } %>
  </form>

  <div class="register-button-wrapper">
    <button type="submit" form="register-form"></button>
  </div>

  <div class="login-link">
    Already have an account? <a href="/registlogin/login">Login here</a>
  </div>
</body>

</html>

<!-- Property of Marco - https://github.com/MarcoBenedictus -->