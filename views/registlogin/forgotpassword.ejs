<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Forgot Password</title>
    <link href="https://fonts.googleapis.com/css2?family=Kanit&display=swap" rel="stylesheet">
    <link rel="icon" type="image/png" href="/images/gamesuitelogo.png">
    <style>
        html,
        body {
            margin: 0;
            padding: 0;
            height: 100%;
            overflow: hidden;
            background: url("/images/userforgotpassword.png") no-repeat center center;
            background-size: contain;
            background-color: #778dfc;
            font-family: 'Kanit', sans-serif;
            position: relative;
        }

        .form-container {
            position: absolute;
            top: 55%;
            left: 21%;
            width: 100%;
            height: 50%;
            display: flex;
            flex-direction: column;
            justify-content: center;
            gap: 20px;
        }

        input {
            font-size: 1.5rem;
            border: none;
            border-radius: 30px;
            padding: 15px 20px;
            background: transparent;
            color: #1b286b;
            outline: none;
            caret-color: black;
            font-family: 'Kanit', sans-serif;
            position: absolute;
            top: -11%;
            left: 0;
            width: 1000px;
            z-index: 1;
        }

        input::placeholder {
            color: #1b286b;
            opacity: 0.8;
        }

        button {
            width: 600px;
            height: 100px;
            background-image: url("/images/recoverpasswordbutton.png");
            background-size: cover;
            background-repeat: no-repeat;
            background-position: center;
            background-color: transparent;
            border: none;
            transition: filter 0.3s ease;
            cursor: pointer;
            position: absolute;
            top: 120px;
            left: 550px;
            transform: translateX(-50%) translateY(-50%);
            z-index: 0;
        }

        button:hover {
            filter: brightness(85%);
        }

        .error-message {
            font-family: 'Kanit', sans-serif;
            color: rgb(192, 0, 0);
            font-size: 1.3rem;
            text-align: left;
            position: absolute;
            top: 10px; /* Adjust this value as needed */
            left: 270px;
            width: 500px;
            transform: translateX(-50%);
            z-index: 2; /* Make sure the error message is on top of other elements */
        }

        .login-link {
            position: absolute;
            bottom: 200px;
            left: 50%;
            transform: translateX(-50%);
            text-align: center;
            font-size: 1.5rem;
            color: white;
            font-family: 'Kanit', sans-serif;
        }

        a {
            color: white;
        }
    </style>
</head>

<body>
    <form id="forgot-form" action="/registlogin/forgotpassword" method="POST">
        <div class="form-container">
            <input type="email" name="email" placeholder="Enter your email" required />
            <% if (typeof error !== 'undefined' && error) { %>
                <p class="error-message"><%= error %></p>
            <% } %>
            <!-- Submit button -->
            <button type="submit"></button>
        </div>
    </form>

    <div class="login-link">
        Remembered your password? <a href="/registlogin/login">Login here</a>
    </div>
</body>

</html>
